# Brambling Note Helm Charts

Add repo to Helm:
```bash
helm repo add brambling-note https://brambling-apps.github.io/helm-chart-actions/
```

Install Helm Chart. For example:
```bash
helm install brambling-note brambling-note/brambling-note-be-ja
va -f values.yaml
```
